import React from 'react';

export default function EventSummary () {

    // get events tied with a user
    // render appropriate information about events with onClick to route to a dynamic page /events/[id].tsx

    return (
        <div>
            EventSummary
        </div>
    )
}